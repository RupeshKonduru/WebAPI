This is for Web API demo

It has login and register page and load data from tables

Step 1 : RUn the WebAPIDemo.sql script in the folder

Step 2: Login in to the application by redirecting to below URL

http://localhost:49261/Login.html

Step 3: Click register and then login you will be redirected to "Demo.html" page

=>It has "GetEmployees" button that fetches the employee names
=>It has "Clear" button that clears the last fetched results.
=>It has "Logout" button ,that redirects to login page and need to relogin to go to that page.

